info:
  name: Me
  type: http
  seq: 4

http:
  method: GET
  url: "{{process.env.API_URL}}/me?full=1"
  params:
    - name: full
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
